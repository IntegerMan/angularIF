import { EntitySortPipe } from './entity-sort.pipe';

describe('EntitySortPipe', () => {
  it('create an instance', () => {
    const pipe = new EntitySortPipe();
    expect(pipe).toBeTruthy();
  });
});
