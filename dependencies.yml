collectors:

- type: js-npm
  path: /
  actors:
  # pull requests for updates to our major version
  - type: js-npm
    versions: "Y.Y.Y"
  # create issues for new major versions
  - type: repo-issue
    versions: "Y.Y.0"
