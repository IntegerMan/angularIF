/**
 * An enum containing a list of comparative weights for entities named after easy to consider objects
 */
export enum EntityWeight {
  feather = 0,
  pocketchange = 1,
  watch = 2,
  cellphone = 3,
  textbook = 4,
  backpack = 5,
  person = 6,
  minivan = 7,
  citybus = 8,
  building = 9,
  cityblock = 10
}
